音楽生成AI　Sunoのプロンプト生成に関するマニュアルを作成する。

公式サイトだけでなく、コミュニティで語られている裏技的なものも網羅する。


## 🚨 絶対厳守ルール - gitignore改変禁止

### .gitignore の取り扱い
- **絶対禁止**: `.gitignore`ファイルの改変・編集・削除
- **理由**: gitignoreに指定されたファイル（`docs/`等）が誤って追跡されると、機密情報や作業ログが公開リポジトリに混入する重大なリスクがある
- **例外**: ユーザーから「.gitignoreを編集して」と明示的な指示があった場合のみ

### ファイルがgit addできない場合の対応
1. **即座に停止**: gitignoreで除外されている可能性を疑う
2. **報告**: 「このファイルはgitignoreで除外されています: [パス]」と報告
3. **確認**: 「このファイルを追跡対象に含めますか？その場合は.gitignoreの修正が必要です」と確認
4. **絶対禁止**: 勝手に`.gitignore`から該当行を削除してgit addを強行

### このプロジェクトのgitignore対象
- `docs/` - 作業ログ、プロンプト開発用ファイル（非公開）
- `.claude/` - Claude Code設定（非公開）
- `CLAUDE.local.md` / `.codex/config.toml` - 個人用の指示・persona（非公開）

`CLAUDE.md` と `AGENTS.md` は非公開対象ではない。両者は追跡対象の共有指示レイヤーであり、
公開して問題ない内容だけを書く（認証情報・絶対パス・個人用ワークフローを含めない）。

**このルールに違反した場合は、即座にgit履歴から完全削除し、謝罪すること**
