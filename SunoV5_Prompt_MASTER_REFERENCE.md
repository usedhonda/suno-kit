# Suno プロンプト・マスターリファレンス（V6 世代）

> **ファイル名に V5 が残っているのは意図的です。** 整合ゲートがこのファイル名を走査リストに
> ハードコードしており、改名するとゲートは GREEN のまま検査対象から静かに外れます。
> 中身は V6 世代のものです。

## この文書の位置づけ

**この文書は事実を自分で主張しません。** 数値も仕様も持たず、**どこを読めばよいか**と
**どう考えればよいか**だけを書きます。

| 層 | 役割 | 実体 |
|---|---|---|
| **正本（事実・数値・証拠ステータス）** | Suno が何を言い、何を言っていないか | `skills/suno/knowledge/` |
| **この文書（読み方・考え方）** | 正本をどう使うか、V6 で何が変わったか | 本ファイル |
| **旧世代アーカイブ** | V5/V5.5 期の観測記録と出典 | `archive/SunoV5_Prompt_MASTER_REFERENCE_v1.5.0.md` |

### なぜ数値を書かないか

この文書には **BPM 域もスライダーの％も文字数上限も記述子の個数も出てきません**
（日付・節番号・引用番号は除く）。理由は2つあります。

1. **V6 の数値は、そもそも Suno が公開していません。** 書こうとすれば推測になります。
2. 正本と同じ数値をここにも書けば、**片方だけ更新されて食い違う**のは時間の問題です。

数値が必要になったら `skills/suno/knowledge/yaml_template.md` を見てください。
そこが唯一の出どころです。

---

## 0. 証拠ステータスの読み方

正本 `skills/suno/knowledge/suno_v6_reference.md` が定める語彙をそのまま使います。
**新しい値を作らないでください。**

| ステータス | 意味 |
|---|---|
| `confirmed_v6` | Suno が V6 について明言した |
| `observed_v6` | V6 で実際に再現した（観測日を記録） |
| `legacy_v55_candidate` | V5.5 では有効だった。V6 では未検証＝仮説 |
| `community_experimental` | コミュニティ報告。未検証。1つずつ A/B する |
| `deprecated` | V6 には当てはまらないと分かっている |
| `unspecified` | Suno が何も公開していない |

本文中では次の記号で示します。
✅ 確認済み / 🧪 V5.5 由来・V6 未検証 / ❓ 公式未記載 / 🔬 コミュニティ報告 / 📦 退役世代

**ラベルのない主張は、この文書のバグです。**

---

## 1. V6 で何が変わったか

V6 は 2026-09-09 にリリースされました [1]。

### 📦 まず前提: 旧モデルは選べません

Suno は **v6 より前の全モデルを退役**させました。作った曲はライブラリに残りますが、
新規生成では選択できません [3]。

この文書の旧版、および `archive/` 配下の記述は、**もう選べない世代についての記録**です。
「V5.5 ではこうだった」は歴史であって、今日の選択肢ではありません。

### ✅ モデルは「版」ではなく「創作モード」で分かれた

V6 は3系統あり、番号の新旧ではなく**意図**で分かれます [1]。

- **精度を取りに行くとき** — 仕上げ、残したいテイク
- **思いつかなかった方向を探すとき** — 予想を外してくる
- **速く回すとき・無料枠** — 下書き、反復

上2つは有料限定です。無料枠で V6 に触れるのは3つ目だけ。
モデル識別子と課金条件は `skills/suno/knowledge/suno_v6_reference.md` を見てください。

**探索 → 収束。** 探索用で方向を見つけ、良かった質を**具体的な言葉に翻訳して**
精度用で仕上げる。「さっきの雰囲気で」ではなく「抑えた近接ボーカル」「緩いドラムの走り」
のように、**何が良かったのかを言語化する**のがこの往復の肝です。

### ✅ 修正は、作り直しではなくなった

**これが V6 で最も実務に効く変更です。**

V5.5 では1語直すにも全曲を作り直す必要がありました。V6 は既存曲の**一部だけ**を
自然文で編集でき、歌詞も語単位で差し替えられます [1]。

だから書き方の重心が移ります。**「どこを変えるか」より「何を保つか」**。
名前を挙げなかったものが保たれること自体が、この機能の価値だからです。

> ⚠️ ただし局所編集が効くのは**自分の Suno 曲**に対してだけです。
> 外部の曲を参照しているだけの場合は使えません。

### ✅ 参照は「素材」ではなく「役割」で渡す

複数のソースを1回の指示でまとめられるようになりました。公式の例自体が
「この曲からボーカル、あの曲からドラム」という**役割の割り当て**になっています [1]。

並べるだけでは足りません。**各ソースに仕事を与え、持ち込みたくないものを明記する。**

### ✅ 入力はテキストだけではない

テキスト・音声・画像・映像を入力にできます [1]。
ここでも**役割を言わないと参照が全体に滲みます**。「この画像は空気感、この音声は旋律、
この文章は主題」と分けて指定してください。

添付の前に権利を確認すること。人の声を参照する場合は本人の同意が要ります。

### ✅ 情景から始めてよい

「真夜中の屋上のような曲」——形容詞を並べるのではなく、**場面そのもの**を起点にできます [1]。
形容詞リストでは届かない質感に届くことがあります。

### ✅ 生成時のコントロールが増えた

- **Variety** — 出力に変化をつけるため、**Suno 側が Style プロンプト自体を調整・更新します**。
  書いた Style をそのまま評価したいときは、これを最小にします [3]。
  つまり**プロンプト文だけを保存しても再現条件にならない**ということです。
- **Max Mode** — 計算資源を多く使うモード。長めの曲、原曲に寄せたい Cover、
  style の転写、全編を通したボーカル・style の一貫性に向くと公式が挙げています [3]。
  クレジットを多く消費します。
- **Simple Mode** — Cover / Remix / Extend のどれを使うべきかを**モデル側が判断**します [3]。
  厳密に歌詞や設定を指定したいときは、自然文に任せない側を使ってください。

### ✅ Custom Models は V6 に載った

自分の曲で V6 を fine-tune できます。既存の Custom Model は**自動的に V6 で動くよう
アップグレード**され、旧モデルで作った曲は再生可能なまま残ります [2][3]。

❓ 一方 **Voices / My Taste / Persona の V6 互換性は公式未記載**です。
同じ「旧来の機能」として一括りにしないでください。片方は確認済み、片方は未記載です。

---

## 2. 書き方の原則（モデル世代に依存しない）

ここに書くのは、V5 でも V6 でも変わらなかった部分です。
**この節はモデルの挙動について何も主張しません。**

### 関係を述べる

属性を並べるのではなく、**属性どうしの関係**を書きます。
どの楽器が主役を担うのか。verse と chorus で何がどう変わるのか。
ボーカルはミックスのどこに座るのか。

長さの問題ではありません。**明示できているかどうか**です。

### 順序は「正体 → 制約」

最初に曲の正体（譲れないもの）、最後に避けたいもの。
途中に編成・ボーカル・構成・空気感を置きます。

### 守りたいものを書く

これは V6 の局所編集で特に効きますが、通常の生成でも同じです。
**変えたいものだけを書くと、変えたくなかったものまで動きます。**

### タグは補助信号

セクションタグ・アノテーションタグは引き続き有効です。
ただし**意味のある指示が第一**で、タグは第二です。
1セクションに要素を詰め込みすぎない。細かく積み上げるより、
生成後に局所編集で直すほうが確実です。

### アーティスト名で指定しない

特定の実在アーティストや実在曲を名指しする指示は通りません。これは V6 でも同じです。
**音響特徴に分解して**書いてください（声の質、演奏の癖、録音の質感、編成）。
これは規約上の要請であると同時に、実務的にも**分解したほうが狙いが定まります**。

### 歌詞フィールドの外に指示を書かない

タグの外に書いたテキストは歌われます。これはモデル世代の話ではなく、
Suno の入力欄の仕組みです。指示はタグの内側に入れてください。

---

## 3. V6 の作業フロー

### 何を持っているかで道が分かれる

| 手元にあるもの | 取れる手段 |
|---|---|
| 自分の Suno 曲 | 局所編集。保つものを明示して、一部だけ変える |
| 権利を持つ音源が複数 | 役割を割り当てて1回の指示でまとめる |
| 外部曲の URL や文章だけ | 通常生成。局所編集も複数ソース結合も使えない |

**ここを飛ばすと、使えない手段を延々と説明することになります。**

### 探索から収束へ

1. 探索用モデルで、**方向の違う複数の案**を投げる（同じ案を引き直すのではなく）
2. 良かったものを選び、**何が良かったのかを具体的な言葉にする**
3. その言葉で精度用モデルに書き直させる
4. 気に入らない箇所は**作り直さず局所編集**で詰める
5. 長さや全体の一貫性が要るなら Max Mode を検討する

### 再現性の考え方が変わった

Variety が Style プロンプトを書き換え得る以上、
**生成テキストだけを保存しても再現条件になりません** [3]。

残すべきは、モデル・モード・各コントロールの値・参照した素材・
実際に送った Style と歌詞——つまり**生成レシピ一式**です。
`skills/suno/knowledge/yaml_template.md` の任意フィールドがこの用途に対応しています。

---

## 4. V5.5 から引き継ぐもの

各ルールについて **keep / modify / demote / retire** の判定が
`skills/suno/knowledge/v55_to_v6_migration.md` にあります。ここでは索引だけ示します。

| 判定 | 中身 |
|---|---|
| **modify**（考え方は活きる、書き方が変わる） | Style の書き方 / モデル選択 / **曲の修正方法** / 複数ソース / 入力の種類 / 情景プロンプト |
| **keep**（前提が変わっていない） | セクション・アノテーションタグ / 文字数上限（V5.5 の数値を使い続ける） / アーティスト名の禁止 |
| **demote**（未検証候補に格下げ） | コミュニティ発の inline タグ / スライダー3種 |
| **📦 V5.5 限定** | Duration / Voices / My Taste / Persona（V6 互換性は未記載） |

🧪 **格下げされたものは削除していません。** V6 では出発点として使い、
結果を見て調整してください。A/B の片側として扱うのが正しい使い方です。

❗ **スライダーを temperature と同一視しないでください。**
Suno の creative control であって、公開されたサンプリング温度ではありません。

---

## 5. 未確定事項

**Suno が公開していないものの一覧は `skills/suno/knowledge/suno_v6_reference.md` にあります。**
ここには転記しません。転記すれば、いずれ片方だけ古くなるからです。

書くときの姿勢だけ示します。

- ❓ 分からないことは「未記載」と書く。**推測値で埋めない**
- サードパーティの API ラッパーが公開している値を、Suno の仕様として持ち込まない
- 他プロジェクトが主張しているだけのモデル識別子を採用しない
- 「たぶん」「〜程度」で数値を作らない

**分からないと書くことは、この文書の失敗ではありません。** 出典のない数値を
書いてしまうことが失敗です。

---

## 6. 旧世代アーカイブ

V5/V5.5 期の記述は `archive/SunoV5_Prompt_MASTER_REFERENCE_v1.5.0.md` に凍結保存しました。
基礎編・応用テクニック編・コミュニティ発見テクニック集・音声条件付けワークフロー・
Deep Research 統合ログ・引用文献の全てが、当時のまま残っています。

そこに含まれるのは:

- V5 期の挙動観測と、それを支える当時の出典
- コミュニティが見つけた裏技の記録
- V5.5 の音声条件付け（Cover / Sample / Inspo）の手順

⚠️ **アーカイブの引用番号は本文書のものと別系統です。** アーカイブ側は当時の番号を
振り直さずに保存してあります。混ぜないでください。

⚠️ アーカイブの基礎編・応用編は**純粋な原文ではありません**。2026-03 に追記が
挿入されています。詳細はアーカイブのヘッダに明記してあります。

---

## 引用文献（V6 一次ソース）

**V5 期の出典はここに載せません。** 当時の URL は V5 期の主張を支えるものであって、
V6 の主張の根拠にはならないからです。それらはアーカイブ側にあります。

| # | 種別 | 出典 | 日付 |
|---|---|---|---|
| [1] | 公式 | https://suno.com/blog/introducing-v6 | 2026-09-09 |
| [2] | 公式 | https://suno.com/release-notes | 2026-09-09 |
| [3] | 公式 | https://help.suno.com/en/articles/13924481 （v6 FAQ） | 2026-09-09 |
| [4] | 独立 | The Verge 初日ハンズオン（🔬 未検証の弱点報告） | 2026-09-09 |

出典の最終照合日と、再照合が必要な条件は
`skills/suno/knowledge/suno_v6_reference.md` の Sources 表に記録しています。

**V6 はサーバ側で動く閉じたモデルです。予告なく変わり得ます。**
Suno の更新があったら、正本から順に照合し直してください。
